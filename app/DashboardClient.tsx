'use client'
// Layer 5 — DashboardClient (PRD §12 step 30): state, fetch /api/posts, render grid vs
// group/cluster views, Scrape button -> POST /api/scrape -> poll status pill.

export function DashboardClient() {
  return <div>{/* TODO: PRD §12 step 30 */}</div>
}
