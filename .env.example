# Non-secret runtime config ONLY (PRD §14).
# API keys are BRING-YOUR-OWN and are entered in the in-app Settings screen,
# stored in the local `settings` table — NEVER here, never in code, never logged.
#
# Copy to `.env.local` (gitignored). The only key that belongs here:
DB_PATH=./research.db
